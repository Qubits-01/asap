import 'package:flutter/material.dart';

class PhoneNumberOtpVerificationScreen extends StatelessWidget {
  const PhoneNumberOtpVerificationScreen({super.key});

  static const routeName = '/phone-number-otp-verification';

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Phone Number OTP Verification Screen'),
      ),
    );
  }
}

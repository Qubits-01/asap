import 'package:flutter/material.dart';

class AppTheme {
  const AppTheme._();

  static final ThemeData lightMode = ThemeData.light(useMaterial3: true);
  static final ThemeData darkMode = ThemeData.dark(useMaterial3: true);
}
